# https://taskfile.dev

version: "3"

vars:
  GREETING: 用于本项目的常用命令编写

tasks:
  default:
    desc: 默认任务
    cmds:
      - echo "${GREETING}"

  toc:
    desc: 生成 Markdown 目录 (TOC)
    cmds:
      - fd . -E vendor -e md | go run cmd/mc-mdtool/main.go toc -i
